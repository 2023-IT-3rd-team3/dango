<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.threeteam.dango.mapper.service.RecommendMapper">
	<select id="plusRecommend" resultType="">
		INSERT INTO	T_RECOMMEND 
		(RECOMMENDID, USERID, BOARDID, RECOMMENDREGISTERDATE, RECOMMENDUPDATEDATE) 
		VALUES 
		(#{recommendid}, #{userid}, #{boardid}, #{recommendregisterdate}, #{recommendupdatedate})
	</select>
	<insert id="minusRecommend">
		DELETE FROM T_RECOMMEND 
		WHERE 
		RECOMMENDID=#{recommendid}
	</insert>
</mapper>