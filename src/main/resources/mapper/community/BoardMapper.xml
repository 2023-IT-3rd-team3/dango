<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.threeteam.dango.mapper.community.BoardMapper">
	<insert id="insertBoard">
		INSERT INTO T_BOARD 
		(BOARDID, BOARDTITLE, USERID, BOARDMAIN, BOARDNOTICE) 
		VALUES 
		(S_BOARD.NEXTVAL, #{boardTitle}, #{userId}, #{boardMain}, #{boardNotice})
	</insert>
	
	<delete id="boardDelete">
		DELETE FROM T_BOARD 
		WHERE BOARDID=#{boardId}
	</delete>
	
	<update id="boardUpdate">
		UPDATE T_BOARD SET 
		BOARDTITLE=#{boardTitle}, BOARDMAIN=#{boardMain} 
		WHERE BOARDID=#{boardId}
	</update>
	
	<select id="getBoard" resultType="boardVO">
		SELECT * FROM T_BOARD 
		WHERE BOARDID=#{boardId}
	</select>	
	
	<select id="getBoardList" resultType="boardVO">
		SELECT * FROM T_BOARD
		WHERE BOARDNOTICE=#{boardNotice}
		ORDER BY BOARDID DESC
	</select>
	
	<select id="communitySearch" resultType="boardVO">
		<where>
			<if test="Title != null">
				AND Title LIKE '%'||#{boardTitle}||'%' 
			</if> 
			<if test="userID != null">
				AND userID LIKE '%'||#{userId}||'%'
			</if>
		</where>
		ORDER BY BOARDID DESC
	</select>
</mapper>