<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.threeteam.dango.mapper.user.UserMapper">
	<insert id="insert">
	INSERT INTO T_USER
	(USERID, USERPW, USERNAME, USERPHONE, USEREMAIL)
	VALUES(#{userid},#{userpw},#{username},#{userphone},#{useremail})
	</insert>
	<select id="select" resultType="userVO">
	SELECT *
	FROM T_USER WHERE (USERID = #{userid}) AND (USERPW = #{userpw})
	</select>
	<select id="selectid" resultType="string">
	SELECT USERID
	FROM T_USER WHERE USEREMAIL = #{useremail} AND USERPHONE = #{userphone}
	</select>
	<select id="selectpw" resultType="string">
	SELECT USERPW
	FROM T_USER WHERE USERID = #{userid} AND USERPHONE = #{userphone}
	</select>
	<delete id="delete">
	DELETE FROM T_USER
	WHERE USERID=#{userid}
	</delete>
	<update id="update">
	UPDATE T_USER
	SET USERNAME= #{username}, USERPHONE= #{userphone}, USEREMAIL= #{useremail}, USERUPDATEDATE=SYSDATE
	WHERE USERID= #{userid}
	</update>
</mapper>