<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    
<mapper namespace="com.threeteam.dango.mapper.recent.RecentMapper">
	
	<select id="isRecent" resultType="recentVO">
		SELECT * FROM T_RECENT WHERE USERID=#{userId} AND WORDID=#{wordId}
	</select>
	<insert id="setRecent">
		INSERT INTO T_RECENT(RECENTID, USERID, WORDID) VALUES(S_RECENT.NEXTVAL,
		#{userId}, #{wordId})
	</insert>
	<update id="updateRecent">
		UPDATE T_RECENT SET RESENTUPDATEDATE=SYSDATE WHERE RECENTID=#{recentId}
	</update>
	<select id="getRecentList">
		SELECT WORDID, WORDWORD AS WORD
		FROM T_RECENT WHERE USERID=#{userId} NATURAL JOIN T_WORD
		ORDER BY RECENTUPDATEDATE DESC
	</select>
</mapper>