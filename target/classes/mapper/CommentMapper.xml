<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.threeteam.dango.mapper.community.CommentMapper">
	<select id="addComment">
		INSERT INTO T_COMMENT 
		(COMMENTID, COMMENTMAIN, USERID, BOARDID, COMMENTREGISTERDATE, COMMENTUPDATEDATE) 
		VLAUES 
		(#{commentId}, #{commentMain}, #{userId}, #{boardId}, #[commentRegisterdate}, #{commentUpdatedate}])
	</select>
	
	<select id="deleteComment">
		DELETE FROM T_COMMENT 
		WHERE BOARDID=#{commentId}
	</select>
	
	<select id="updateComment">
		UPDATE T_COMMENT SET COMMENTMAIN=? 
		WHERE 
		COMMENTID=#{commentId}
	</select>
	
	<select id="getComment">
	
	</select>
	
	<select id="getCommentList">
	
	</select>
</mapper>